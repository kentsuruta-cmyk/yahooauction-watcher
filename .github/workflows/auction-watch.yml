name: auction-watch

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: 終了間際のオークションを確認して通知する
        run: curl -fsS -m 60 "https://yahooauction-watcher.vercel.app/api/watch-check"
